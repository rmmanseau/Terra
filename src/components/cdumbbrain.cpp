#include "cdumbbrain.h"

SRC_FACTORY_REGISTER(CDumbBrain);

bool CDumbBrain::init(YAML::Node node)
{
    // try
    // {
    //     // Do stuff here ...
    //     // thing = node["thing"].as<thing>();
    //     // stuff = Stuff(nodes["stuff"][0].as<stuff>(),
    //     //               nodes["stuff"][1].as<stuff>());
    // }
    // catch (...)
    // {
    //     return false;
    // }

    return true;
}

// Other Functions
